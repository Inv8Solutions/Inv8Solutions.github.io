import type { Config } from 'tailwindcss'

export default {
  theme: {
    extend: {
      fontFamily: {
        'general-sans': ['GeneralSans', 'system-ui', 'sans-serif'],
      },
    },
  },
} satisfies Config
